1，每个网页里面有自己的编码，指定网页内部的字符编码。
2，网页到jsp或者servlet，是通过iso8859-1编码传输的。
3，用get方式，request和response的set编码是无效的，不能通过它来改变编码，通过new String("", "");
	post方式，则可以通过set来设置编码
4，每个web工程都有自己的编码，但数据传递过程中与他没有关系
5，数据库的数据也有他的编码。

只有当以上所有的编码集全部一样活着兼容时，才能正常的显示，通常设置为utf-8即可