Android网络编程主要是关于Http协议的，关于网络的部分需要放在子线程里，
因为关于网络的操作是比较耗时的，放在主线程里会影响主线程里的工作效率，
但子线程不能对界面做操作，所以需要借助消息机制，Android的消息机制与Windows
的差不多，有消息队列和消息循环，用Handler可以发送和处理消息，Handler处理消息的
模式类似于W32的消息处理，用一个回调函数来分离各个消息。
而Android的按钮响应则类似于MFC的消息映射函数的处理机制。