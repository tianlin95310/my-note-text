java从JDK1.2版本开始，就把对象的引用分为4中级别，依次为强引用，软引用，弱引用和虚引用。

强引用：使用的内容不可被回收，知道出现内存溢出

软引用：如果对象只具有软引用，那么空间足够的情况下，垃圾回收器就不会回收他

弱引用：如果对象只具有弱引用，那么一旦垃圾回收器发现了它，立马就会回收，不管当前空间
	是否足够

虚引用：如果对象只具有虚引用，在任何时候都有可能被回收，必须和引用队列联合使用



另外还有一个硬引用，Lrucache，用于缓存机制

总结：
若只是想避免内存溢出，用软引用即可，若高提高性能，则可以使用弱引用