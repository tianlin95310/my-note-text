1，Java里面的引用相当于C语言的指针，引用存在栈空间，他自己要占用栈内存，存的内容是对象在
堆空间（或是常量区）的地址。

2，对象进行 == 比较时，比较的是他们指向的那块内存的地址，也就是比较他们自身的值，
但是他们本身的地址是无法比较的。

3，要比较引用指向的内容，需要重写Object类的equals函数。若两个对象用equals比较相等，并不能得到两个对象就是一样的
因为可能有两个对象他们的内容相同。

	