如果定义时没有赋初值，那么在构造函数里一定要赋初值
这个与c++一样，c++是在构造函数初始化列表里实现的

1，每一个构造函数里面都要初始化
class My
{
	final int a ;		// 可以不立即初始化，但需要在构造函数里初始化
	
	// 对于常量态的属性，每一个构造函数中都要有初始化
	My(int b)
	{
		this.a = b;
	}
	
	My()
	{
		this.a=1;
	}
}