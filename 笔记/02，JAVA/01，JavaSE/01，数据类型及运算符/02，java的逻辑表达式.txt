1，&，|，^,（与，或，亦或）
	1，这3个运算符既支持位运算，也支持逻辑运算。

	2，做位运算的结果是一个数，数值不能参与逻辑运算的直接参数，但C语言可以
		C语言可以while(1),java只能while(true)
	3，做逻辑运算时，结果为Boolean型，其操作数也必须是Boolean型，若有多个操作数
		每个都会执行

2，&&，||，！
	1，他们只支持逻辑运算，操作数只能是Boolean类型。
	2，若有多个操作数，一旦能确定最终结果，后面的就不做了。

3，关系运算符的运算结果也为bool类型。

4，java里不支持逗号运算符，但for循环里可以使用