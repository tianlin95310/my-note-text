1,重载形式
类的成员函数，和顶层函数，java里面有与操作符重载类似的对象比较器

2，重载为顶层函数时，其中一个成员必须是类类型若要访问私有成员还要借助friend关键字。

3，[],=,(),->,只能重载为成员函数，且=重载后不能被继承。

4，成员选择符（.）,成员对象选择符（.*）,:: , ?: ,不能被重载

5，重载流对象时，参数需要引用类型,否则返回的流与调用的流无关系

6，重载为成员函数时，重载函数的参数一般比操作符的运算对象少一个，少的一个是调用该
   operate op函数的对象。