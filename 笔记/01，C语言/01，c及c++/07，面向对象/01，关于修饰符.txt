C语言共有3种修饰符。Java有4种。

c语言源文件都在一个文件夹，所以没有java那么复杂。

1，public
	用对象可以直接访问

2，private
	只能在类的内部使用，不能用对象直接访问

3，protected
	弥补private成员继承时，子类不可见的问题，他与Java的protected不同
	protected修饰时，本类对象不可以直接访问，只能在类内部访问，保持了封装性

